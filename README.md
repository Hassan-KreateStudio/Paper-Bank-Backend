# Paper Bank Backend

Minimal Bun + Hono + TypeScript backend scaffold.

## Getting started

1. Install Bun if needed: `curl -fsSL https://bun.sh/install | bash`
2. Install dependencies: `bun install`
3. Start the dev server: `bun run dev`

The app listens on `PORT` or defaults to `3000`.
